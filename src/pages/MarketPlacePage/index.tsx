import { FC } from 'react';
import styles from './styles.module.less';

const MarketPlacePage: FC = () => {
  return <div className={styles.root}>Market place</div>;
};

export default MarketPlacePage;
